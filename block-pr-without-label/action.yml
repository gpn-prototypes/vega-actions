name: Generate changelog
description: Uses lerna-changelog to generate the changelog from a tag to another

inputs:
  repo_token:
    description: Repository token from github
    required: true
  necessary_label:
    description: Label when block pr merging
    required: true

runs:
  using: node12
  main: dist/index.js

branding:
  icon: clock
  color: yellow
